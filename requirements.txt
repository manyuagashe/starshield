# StarShield Prediction Layer Dependencies

# Core FastAPI and async support
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data validation and settings
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Data processing (for integration with Model/Query layers)
pandas>=2.1.0
numpy>=1.24.0

# Caching backends
aioredis>=2.0.1  # Optional: for Redis caching
aiosqlite>=0.19.0  # Optional: for async SQLite support

# Development and testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0  # For testing FastAPI endpoints
pytest-cov>=4.1.0

# Optional: for production deployment
gunicorn>=21.0.0

# Type checking and linting (development)
mypy>=1.7.0
black>=23.0.0
isort>=5.12.0
flake8>=6.1.0